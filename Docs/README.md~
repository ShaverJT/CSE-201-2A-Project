CSE-201-2A-Project
==================
AppCat is a catalogue of Apps that has a Django
web framework as a back end with HTML implementation
for the front end. 

Currently, the HTML and Django are linked, but we are
in the process of updating the HTML to look nicer. 